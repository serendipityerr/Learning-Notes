## Some useful TMUX instructions

1. List all: `tmux ls`

2. Create new session: `tmux new -s name`

3. Kill session: `tmux kill-session -t name`

4. Attach specific session: `tmux attach -t name`

5. Switch to specific session: `tmux switch -t name`